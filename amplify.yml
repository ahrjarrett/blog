version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
        - yarn build
    build:
      commands:
        - yarn serve
  artifacts:
    baseDirectory: public
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
